# season 0.3.16.9000

* Added a `NEWS.md` file to track changes to the package.

## Breaking change

* `createAdj()` argument, `filepath` no longer defaults to "Adj.txt", and will error if no filepath is provided